const endpoint = "http://localhost:5000/api";

const status = {
  pending: "PENDING",
  approved: "APPROVED",
  rejected: "REJECTED",
};

const leaveType = {
  unpaid: "Unpaid",
};

export { endpoint, status, leaveType };
